---
myst:
  html_meta:
    description: "Why the matverse object has this shape, what it deliberately does not build, and what is still open."
---

# Design

The design document lives at the repository root as `DESIGN.md` and is included
here verbatim, so the two never drift.

```{include} ../../DESIGN.md
:heading-offset: 1
```
