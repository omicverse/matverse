# API Reference

Import matverse as:

```python
import matverse as mv
```

The API page is generated from the registry, so it lists exactly what the
library exposes — and nothing that was added without a decorator.

```{toctree}
:maxdepth: 2

user
```
